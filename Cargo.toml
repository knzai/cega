[package]
name = "cega"
description = "CGA and EGA binary image file processing library"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/knzconnor/cega"

[dev-dependencies]
rusty-hook = "^0.11.2"

[dependencies]
bitvec = "1"

# [features]
# default = ["termart"]
# termart = ["dep:crossterm", "dep:ratatui"]

[dependencies.sdl2]
version = "0.37.0"
optional = true
default-features = false
features = ["gfx"]

# [dependencies]
# crossterm = { version = "0.27.0", optional = true }
# ratatui = { version = "0.24.0", optional = true }